# Intentionally empty: test_packaging.jl's "only the module root includes by path"
# rule means Qritical.jl itself must include ids.jl/node.jl/wire.jl/leg.jl/attachment.jl
# directly, in that order. This file is a placeholder in case topograph.jl grows content
# of its own (e.g. an AbstractGenTopoGraph-level docstring or shared helpers) later.
