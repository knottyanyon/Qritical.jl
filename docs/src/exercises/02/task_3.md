```@meta
EditURL = "task_3.jl"
```

# 3. Mixed Canonical State

!!! question "Task 2.3 — Mixed Canonical State"
    Write a function that performs a mixed canonical decomposition of the state `psi.jls` of [Exercise 1](@ref "Task 1.3 — SVD a state"). Allow for an arbitrary site ``l`` where the state changes from left to right normalized. The function should allow for a maximum matrix dimension ``D`` to truncate the state after each SVD.

---

*This page was generated using [Literate.jl](https://github.com/fredrikekre/Literate.jl).*

